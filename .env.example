# Mode d'exécution du backend (production recommandé en déploiement).
NODE_ENV=production

# Port interne exposé par l'API et l'application Angular.
PORT=3000
# Port HTTP exposé par le frontend Angular.
FRONTEND_PORT=8080

# Chaîne de connexion Prisma vers PostgreSQL (utilisée par le backend).
DATABASE_URL=postgresql://postgres:postgres@db:5432/le-budget?schema=public

# Identifiants PostgreSQL injectés dans le conteneur de base de données.
POSTGRES_USER=postgres
# Mot de passe du compte PostgreSQL ci-dessus.
POSTGRES_PASSWORD=postgres
# Nom de la base créée lors du démarrage du conteneur PostgreSQL.
POSTGRES_DB=le-budget

# Secret utilisé pour signer les JWT d'accès.
JWT_SECRET=change-me
# Durée de validité des tokens d'accès.
JWT_EXPIRES_IN=1h
# Durée de validité des tokens de rafraîchissement.
JWT_REFRESH_EXPIRES_IN=30d

# URL du backend injectée dans le frontend au démarrage du conteneur.
FRONTEND_API_BASE_URL=http://backend:3000
