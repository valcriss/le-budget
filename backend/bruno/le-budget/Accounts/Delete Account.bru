meta {
  name: Delete Account
  type: http
  seq: 5
}

delete {
  url: {{API_URL}}/accounts/{{MAIN_ACCOUNT}}
  body: none
  auth: inherit
}

script:post-response {
  let data = res.getBody();
  bru.setVar("MAIN_ACCOUNT", "deleted", { persist: true });
}

settings {
  encodeUrl: true
  timeout: 0
}
