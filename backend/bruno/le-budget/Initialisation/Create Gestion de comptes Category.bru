meta {
  name: Create Gestion de comptes Category
  type: http
  seq: 8
}

post {
  url: {{API_URL}}/categories
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Gestion de comptes",
    "kind": "EXPENSE",
    "parentCategoryId": "{{FACTURE_CATEGORY_ID}}"
  }
}

tests {
  let data = res.getBody();
  bru.setEnvVar("CHILD_CATEGORY_ID", data.id, { persist: true });
}

settings {
  encodeUrl: true
  timeout: 0
}
