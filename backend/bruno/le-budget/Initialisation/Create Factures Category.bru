meta {
  name: Create Factures Category
  type: http
  seq: 2
}

post {
  url: {{API_URL}}/categories
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Factures",
    "kind": "EXPENSE"
  }
}

script:post-response {
  let data = res.getBody();
  bru.setVar("FACTURE_CATEGORY_ID", data.id, { persist: true });
}

settings {
  encodeUrl: true
  timeout: 0
}
