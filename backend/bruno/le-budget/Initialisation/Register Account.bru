meta {
  name: Register Account
  type: http
  seq: 1
}

post {
  url: {{API_URL}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "{{VALID_EMAIL}}",
    "password": "{{VALID_PASSWORD}}",
    "displayName": "Daniel Silvestre"
  }
}

script:post-response {
  let data = res.getBody();
  bru.setVar("JWT", data.accessToken, { persist: true });
}

settings {
  encodeUrl: true
  timeout: 0
}
