meta {
  name: Delete Parent Category
  type: http
  seq: 7
}

delete {
  url: {{API_URL}}/categories/{{PARENT_CATEGORY_ID}}
  body: none
  auth: inherit
}

script:post-response {
  bru.setVar("PARENT_CATEGORY_ID", "deleted", { persist: true });
}

settings {
  encodeUrl: true
  timeout: 0
}
